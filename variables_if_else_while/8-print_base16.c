#include <stdio.h> 
/**
 *  * main - hexadecimal
 *   * Return: always return 0
 *    */

int main(void)
{
int i = 0;
  
  while (i < 16) {
	      putchar (i);
	          i++;
		    }
    
    return 0;
}
